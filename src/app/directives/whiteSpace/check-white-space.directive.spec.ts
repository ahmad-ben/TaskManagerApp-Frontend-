import { CheckWhiteSpaceDirective } from './check-white-space.directive';

describe('CheckWhiteSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckWhiteSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
